# 工具执行边界

- `/data`只读；运行环境、依赖、临时文件、脚本和结果写入`/workspace`。
- 案件材料、字段值和分析结果不得发送到外部服务。联网仅用于获取通用解析依赖。
- 可按任务使用命令行、pandas、openpyxl或`execute_code`，也可并行、批量或后台运行；后台任务结束后检查退出状态和产物。
- 大输出先写入`/workspace`，对话只读取完成任务所需的结构、计数、有限样本、有限聚合和候选定位。
